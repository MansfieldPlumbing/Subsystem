namespace Subsystem.Cm;

// In Memoriam — a LOCKED INVARIANT, on the same footing as the telos. This is the project's permanent
// dedication: load-bearing, not decoration. SS023 (the analyzer gate) enforces it — remove this const,
// rename it, or strip her name or her years and the build goes RED. It cannot be abridged out.
//
// `ss onboard` renders it as the solemn advisory that opens every onboarding; the README and the About
// screen carry it too. This const is the single source of truth they all draw from.
public static class Dedication
{
    public const string InMemoriam =
        "This project is devoted to the loving memory of Billie Dean Mansfield — 1945–2026.";
}
